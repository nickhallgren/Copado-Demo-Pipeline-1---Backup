<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Extracted_Year__c</fullName>
    <description>HA: This will extract the year from start and end date based on a condition</description>
    <externalId>false</externalId>
    <formula>IF(
   AND(
       MONTH(StartDate) = 12,
       DAY(StartDate) &gt;= 16
   ),
   YEAR(EndDate),
   YEAR(StartDate)
)</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>Extracted Year</label>
    <precision>18</precision>
    <required>false</required>
    <scale>2</scale>
    <trackHistory>false</trackHistory>
    <type>Number</type>
    <unique>false</unique>
</CustomField>
