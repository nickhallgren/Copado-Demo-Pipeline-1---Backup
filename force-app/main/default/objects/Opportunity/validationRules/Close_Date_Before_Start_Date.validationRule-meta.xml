<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Close_Date_Before_Start_Date</fullName>
    <active>false</active>
    <description>The Close Date must occur before the Start Date</description>
    <errorConditionFormula>AND( Probability &gt;= 0.25,
  OR(ISBLANK(CloseDate),
  ISBLANK(Start_Date__c),
  CloseDate&gt;= Start_Date__c)
  )</errorConditionFormula>
    <errorMessage>The Close Date must occur before the Start Date</errorMessage>
</ValidationRule>
