<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Start_Date_Before_End_Date</fullName>
    <active>false</active>
    <description>The start date must always before the end date if probability is &gt;= 25%</description>
    <errorConditionFormula>AND( Probability &gt;= 0.25,
OR(ISBLANK(Start_Date__c),
ISBLANK(End_Date__c),
Start_Date__c &gt;= End_Date__c)
)</errorConditionFormula>
    <errorMessage>The Start Date must be before the End Date.</errorMessage>
</ValidationRule>
