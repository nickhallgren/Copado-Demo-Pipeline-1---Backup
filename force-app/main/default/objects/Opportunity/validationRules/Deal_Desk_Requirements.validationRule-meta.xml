<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Deal_Desk_Requirements</fullName>
    <active>false</active>
    <description>Requires user to add a link to at least on of the like areas under deal desk if they request a review</description>
    <errorConditionFormula>AND(
NOT(ISBLANK(TEXT(Deal_Desk_Tier__c))),

OR(ISBLANK(Scope__c),ISBLANK(Margin_Calc_Link__c),
AND(ISBLANK(SOW__c),ISBLANK(Proposal__c))

)
)</errorConditionFormula>
    <errorMessage>To request a review you must enter a link in the Scope and Margin Calculator fields and either SOW or Proposal.</errorMessage>
</ValidationRule>
