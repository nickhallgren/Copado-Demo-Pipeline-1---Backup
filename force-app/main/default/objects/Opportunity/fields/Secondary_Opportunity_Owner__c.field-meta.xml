<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Secondary_Opportunity_Owner__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Used to captured the name (user record) of a secondary opportunity owner in the occurrence the opportunity is being jointly sold (i.e. Account Executive &amp; Account Manager working together, or Account Executive &amp; Partner Sales jointly selling together)</description>
    <inlineHelpText>Used to captured the name (user record) of a secondary opportunity owner in the occurrence the opportunity is being jointly sold (i.e. Account Executive &amp; Account Manager working together, or Account Executive &amp; Partner Sales jointly selling together)</inlineHelpText>
    <label>Secondary Opportunity Owner</label>
    <referenceTo>User</referenceTo>
    <relationshipName>SecondaryOpportunities</relationshipName>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Lookup</type>
</CustomField>
