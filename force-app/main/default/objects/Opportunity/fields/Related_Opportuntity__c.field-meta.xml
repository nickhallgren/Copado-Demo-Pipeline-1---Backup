<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Related_Opportuntity__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>Used for change orders to link the original Opportunity this change order came from.</description>
    <inlineHelpText>Used to manually look up the original Opportunity this change order is created from.</inlineHelpText>
    <label>Related Opportuntity</label>
    <referenceTo>Opportunity</referenceTo>
    <relationshipLabel>Change Orders</relationshipLabel>
    <relationshipName>Opportunities</relationshipName>
    <required>false</required>
    <trackFeedHistory>false</trackFeedHistory>
    <trackHistory>false</trackHistory>
    <type>Lookup</type>
</CustomField>
