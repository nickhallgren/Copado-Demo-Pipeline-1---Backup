<?xml version="1.0" encoding="UTF-8"?>
<ValidationRule xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>LeaveEndDate</fullName>
    <active>true</active>
    <description>The leave end date field cannot be black if Status change field includes &quot;status Change</description>
    <errorConditionFormula>INCLUDES(Contact_Status_Change__c, &quot;On Leave&quot;) &amp;&amp; 
OR(ISBLANK(Leave_End_Date__c), ISBLANK(Leave_Start_Date__c))</errorConditionFormula>
    <errorMessage>If Contact Status Change includes &quot;On Leave&quot;,  &quot;Leave Start Date&quot; and &quot;Leave End Date&quot; need to be populated.</errorMessage>
</ValidationRule>
